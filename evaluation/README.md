# Evaluation Dataset

The evaluation set is synthetic and labelled for test design only. It contains no official BIS facts and must not be used as production knowledge. Each case defines an expected behavior category, language, and whether the system should require evidence or refuse.

Do not publish accuracy, recall, citation, or latency values until an executable evaluator has run against a versioned knowledge snapshot and its measured output has been reviewed.
